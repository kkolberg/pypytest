# pypytest

for make do


after cloning do

1. cd into folder
2. 

    ```sh
    which pypy
    virtualenv -p <path to pypy> .venv
    source .venv/bin/activate
    pip install -r requirements.txt
    ```